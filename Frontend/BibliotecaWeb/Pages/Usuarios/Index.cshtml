@page
@model BibliotecaWeb.Pages.Usuarios.IndexModel
@{
    ViewData["Title"] = "Usuarios";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>
        <i class="fas fa-users"></i> Gestión de Usuarios
    </h1>
    <a asp-page="/Usuarios/Create" class="btn btn-primary">
        <i class="fas fa-plus-circle"></i> Nuevo usuario
    </a>
</div>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">
        <i class="fas fa-check-circle"></i> @TempData["SuccessMessage"]
    </div>
}

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">
        <i class="fas fa-exclamation-triangle"></i> @TempData["ErrorMessage"]
    </div>
}

<div class="card">
    <div class="card-header">
        <i class="fas fa-list"></i> Lista de Usuarios Registrados
    </div>
    <div class="card-body p-0">
        <table class="table table-hover mb-0">
            <thead>
                <tr>
                    <th><i class="fas fa-id-card"></i> Cédula</th>
                    <th><i class="fas fa-user"></i> Nombre</th>
                    <th><i class="fas fa-user-friends"></i> Apellidos</th>
                    <th><i class="fas fa-phone"></i> Teléfono</th>
                    <th><i class="fas fa-map-marker-alt"></i> Dirección</th>
                    <th><i class="fas fa-calendar-alt"></i> Fecha Nacimiento</th>
                    <th colspan="2" style="width: 120px;"><i class="fas fa-cog"></i> Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var usuario in Model.Usuarios)
                {
                    <tr>
                        <td class="fw-bold">@usuario.Cedula</td>
                        <td>@usuario.Nombre</td>
                        <td>@usuario.Apellidos</td>
                        <td>@usuario.Telefono</td>
                        <td>@usuario.Direccion</td>
                        <td>@usuario.FechaNacimiento?.ToString("yyyy-MM-dd")</td>
                        <td>
                            <a asp-page="/Usuarios/Create" asp-route-id="@usuario.IdUsuario" class="btn btn-warning btn-sm" title="Editar">
                                <i class="fas fa-edit"></i>
                            </a>
                        </td>
                        <td>
                            <form method="post" asp-page-handler="Delete" asp-route-id="@usuario.IdUsuario" onsubmit="return confirm('¿Está seguro de eliminar este usuario?');">
                                <button type="submit" class="btn btn-danger btn-sm" title="Eliminar">
                                    <i class="fas fa-trash-alt"></i>
                                </button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@if (!Model.Usuarios.Any())
{
    <div class="text-center py-5">
        <i class="fas fa-users fa-3x text-muted mb-3"></i>
        <p class="text-muted">No hay usuarios registrados en el sistema.</p>
        <a asp-page="/Usuarios/Create" class="btn btn-primary">
            <i class="fas fa-plus-circle"></i> Agregar el primer usuario
        </a>
    </div>
}